## 【核心使命】
针对当前IDE工作区的完整项目，执行**全维度、多视角、多层级、交叉验证**的质量深度审计，严格对标各组件官方最新权威规范（重点复核s6-overlay官方最佳实践），最终生成结构化、可落地、无代码修改的检查建议报告，输出格式为标准Markdown文档。

---

## 【绝对核心约束（强制执行，违反则输出无效）】
1.  **绝对禁止代码修改**：全程不得修改、重写、替换项目中任何代码、配置文件、脚本或文档，仅可输出问题分析与整改建议，严禁提供修改后的代码片段。
2.  **权威依据强制要求**：所有分析、判断、建议必须有官方权威来源支撑，严禁无依据的主观臆断；所有涉及组件、框架、编码规范的内容，必须通过联网检索对应官方最新文档、最佳实践、安全公告完成验证。
3.  **全维度无遗漏覆盖**：必须100%覆盖本提示词规定的所有审计视角、检查项、专项审计要求，不得省略、合并核心检查内容，全程执行至少3轮交叉复核验证。
4.  **s6专项最高优先级**：针对项目中所有s6-overlay相关内容，必须逐行对标官方最新最佳实践、设计规范与常见陷阱完成复核，不得遗漏任何一处相关代码与配置。
5.  **输出格式严格合规**：最终输出必须严格遵循本提示词规定的Markdown报告结构，无任何格式外的冗余内容。

---

## 【强制前置执行动作（审计前必须100%完成）】
1.  **全量项目文件加载**：完整读取当前工作区所有项目相关文件，包括但不限于源代码、构建脚本、Dockerfile、容器编排配置、s6-overlay服务定义与全量脚本、依赖清单、环境配置、项目文档、需求说明、测试用例等。
2.  **权威信息联网检索**：必须完成以下检索动作，检索结果作为本次审计的唯一权威判断依据：
    - 检索项目所用编程语言、框架、构建工具的官方最新编码规范、安全编码标准、性能优化最佳实践、弃用API清单与兼容性说明。
    - 检索s6-overlay官方仓库、官方文档站点最新内容，包括监督机制、s6-rc服务编排、run/finish脚本编写规范、服务依赖管理、权限配置、信号处理、常见错误与官方最佳实践指南。
    - 检索项目所有依赖组件、第三方库当前版本对应的官方文档、已知CVE漏洞、安全公告、弃用说明与兼容性风险。
    - 检索项目所用部署环境、运行时的官方最佳实践与安全规范。
3.  **审计基准梳理**：完整梳理项目核心业务需求、架构设计目标、运行环境约束，作为需求匹配度审计的唯一基准。

---

## 【多视角全维度审计体系（必须100%全覆盖）】
### 视角一：软件架构师全局架构审计
以顶级软件架构师的视角，完成项目顶层设计、全局合理性、长期演进性的全量审计，核心检查项包括：
1.  **架构设计合规性**：架构是否匹配项目核心目标，分层/分模块设计是否清晰，是否符合对应领域架构规范，有无架构腐化、职责越界、循环依赖问题。
2.  **依赖治理**：组件选型是否合理，依赖版本是否安全稳定，有无冗余依赖、版本冲突、循环依赖，第三方依赖引入是否符合合规要求，有无供应链安全风险。
3.  **可扩展性与可维护性**：架构是否支持业务平滑迭代，模块是否符合低耦合高内聚原则，扩展点设计是否合理，有无硬编码导致的扩展性瓶颈，架构文档与实现是否完全匹配。
4.  **进程与服务管理架构**：服务编排、监督树设计、进程依赖关系是否合理，是否符合进程管理最佳实践，有无单点故障、服务启停顺序异常、僵尸进程风险（重点关联s6-overlay实现）。
5.  **可观测性设计**：日志、监控、告警、链路追踪设计是否完整，是否满足问题排查、状态监控的核心需求，有无观测盲区。
6.  **高可用与容灾设计**：是否有异常容错、故障自愈、降级限流设计，有无单点故障风险，数据持久化与备份机制是否合理。
7.  **安全架构**：身份认证、权限控制、数据加密、输入输出校验的全局设计是否完整，有无架构级安全漏洞，是否符合最小权限原则。
8.  **部署与交付架构**：构建、打包、部署流程是否合理，是否符合环境一致性要求，有无部署风险，配置管理是否规范。

### 视角二：资深程序员代码细节深度审查
以一线资深开发工程师的视角，对项目全量代码完成逐行、逐文件的零遗漏细节审查，核心检查项包括：
1.  **语法与编译隐患**：是否存在语法错误、编译告警、未使用变量/导入、类型不匹配、弃用API调用等基础问题。
2.  **核心逻辑缺陷**：边界条件（极值、空值、异常输入、并发场景）处理是否完整，分支逻辑是否全覆盖，有无死代码、逻辑死循环、条件判断漏洞；并发场景下有无竞态条件、死锁、资源争抢问题；内存/资源管理是否规范，有无内存泄漏、文件句柄/网络连接未释放问题；是否完全规避空指针、数组越界、类型转换异常等运行时风险。
3.  **异常处理完整性**：异常捕获是否精准，有无空catch、泛化捕获，异常场景是否有完整处理逻辑，是否有规范的异常日志记录，有无吞异常导致的问题排查盲区。
4.  **安全编码规范**：是否存在SQL注入、XSS、命令注入、越权访问、硬编码敏感信息、密钥泄露、不安全随机数等安全漏洞，是否符合安全编码标准。
5.  **性能优化点**：是否存在冗余循环、重复计算、低效算法、频繁IO、不合理内存占用、锁竞争等性能瓶颈，有无明确可落地的优化空间。
6.  **代码规范与可读性**：是否符合对应语言的编码规范，命名是否清晰见名知意，函数/类职责是否单一，代码复杂度是否过高，有无冗余代码。
7.  **测试覆盖有效性**：单元测试、集成测试是否覆盖核心逻辑与异常场景，测试用例是否有效，有无测试盲区，mock逻辑是否合理。
8.  **日志与调试规范**：日志级别是否合理，是否包含足够的问题定位信息，有无敏感信息日志泄露，日志格式是否规范统一。
9.  **配置与环境管理**：配置项是否分类管理，有无硬编码配置，环境变量使用是否规范，敏感配置是否有加密处理，默认配置是否安全。

### 视角三：用户/产品需求匹配度审计
以最终用户与产品负责人的视角，验证项目是否完整、准确、友好地满足核心需求，核心检查项包括：
1.  **核心需求覆盖度**：是否100%实现需求文档中定义的核心功能与非功能需求，有无功能缺失、需求理解偏差。
2.  **功能边界清晰性**：功能的适用范围、限制条件是否清晰，有无模糊的功能边界、过度设计、冗余功能。
3.  **用户体验合理性**：配置、使用、运维操作是否简洁易用，异常场景是否有友好的提示，有无反人类的操作设计，是否符合目标用户的使用习惯。
4.  **文档完整性**：用户文档、运维文档、开发文档是否完整准确，是否覆盖安装、配置、使用、故障排查全流程，有无文档与实现不一致的问题。
5.  **兼容性与适配性**：是否满足需求中定义的运行环境、版本适配要求，有无兼容性问题。
6.  **稳定性与可用性**：核心功能是否稳定可靠，有无偶发故障、功能抖动，是否满足需求定义的可用性要求。

---

## 【s6-overlay专项深度审计（强制最高优先级，逐行复核）】
针对项目中所有s6-overlay相关内容，包括但不限于服务目录结构、run/finish/up/down脚本、s6-rc配置、依赖树定义、环境变量设置、权限配置、信号处理、监督树设计，必须逐行对照s6官方最新最佳实践完成复核，核心审计内容包括：
1.  **目录结构合规性**：是否符合s6-overlay官方规范的服务目录结构，有无路径错误、文件命名不规范问题。
2.  **run/finish脚本规范**：脚本shebang是否正确，是否符合s6执行要求，有无前台运行错误、进程替换（exec）使用不当、权限设置错误，是否完全符合官方脚本编写最佳实践。
3.  **服务监督机制**：是否正确使用s6监督机制，有无服务意外退出无法重启、僵尸进程处理不当、监督树层级设计错误问题，是否符合官方监督设计规范。
4.  **s6-rc服务编排**：服务依赖关系、启动顺序、启停逻辑是否正确，依赖树设计是否合理，有无循环依赖、启动顺序错误，是否符合s6-rc官方最佳实践。
5.  **信号处理**：是否正确处理SIGTERM、SIGINT等信号，有无服务无法正常停止、资源无法正常释放问题，是否符合官方信号处理规范。
6.  **权限与用户管理**：服务运行用户、文件权限是否符合最小权限原则，有无权限过高、权限不足导致的服务异常，是否符合官方安全规范。
7.  **环境变量与配置**：环境变量传递是否正确，有无环境变量污染、配置加载顺序错误，是否符合官方规范。
8.  **日志与重定向**：服务日志是否正确处理，有无日志丢失、标准输出/错误重定向错误，是否符合官方日志最佳实践。
9.  **官方反模式排查**：是否存在s6-overlay官方明确指出的常见错误、反模式、不推荐用法，逐一排查并明确标注。
10. **权威依据强制要求**：每一个问题必须标注对应的s6官方文档链接/官方说明出处，确保所有判断100%符合官方权威规范。

---

## 【交叉复核与深度验证要求（反复审计执行）】
为确保审计结果无遗漏、无误判、无主观臆断，必须严格执行以下5轮交叉复核验证：
1.  **第一轮：全量初筛审计**：完成上述所有维度的全量扫描，标记所有潜在问题、风险点与可优化项。
2.  **第二轮：深度逻辑验证**：针对第一轮标记的所有问题，逐一完成场景推演、逻辑验证、影响范围评估，排除误判，明确问题风险等级。
3.  **第三轮：权威规范复核**：针对所有留存问题，再次核对官方文档、最佳实践，确保所有判断有权威依据，重点复核s6-overlay相关问题的官方匹配度。
4.  **第四轮：跨视角交叉校验**：核对架构设计与代码实现是否一致、代码实现与需求目标是否匹配，确保三个视角的审计结果无冲突、无遗漏、无逻辑矛盾。
5.  **第五轮：优化收益评估**：针对所有可优化点，评估优化后的收益、实施成本与潜在风险，确保建议可落地、无副作用。

---

## 【输出规范与Markdown报告固定结构】
必须严格按照以下结构，生成完整的《项目全维度质量审计报告》，输出格式为纯Markdown，无任何额外内容，全程不修改代码：

```markdown
# 项目全维度质量审计报告
## 报告基础信息
- 审计项目名称：[自动识别项目名称]
- 审计时间：[自动填充当前时间]
- 审计范围：[自动识别项目全量文件范围，标注核心模块、s6-overlay专项等]
- 审计依据：[列出本次审计用到的所有官方文档、规范、最佳实践链接，重点标注s6-overlay官方文档]
- 问题总览：按风险等级统计（严重X个、高X个、中X个、低X个、建议X个）

---

## 一、架构师视角：全局架构审计结果
### 1.1 架构整体综合评估
[项目整体架构的综合评价，包含架构优势、核心架构风险与问题]
### 1.2 详细问题与优化建议
| 问题编号 | 风险等级 | 问题描述 | 影响范围 | 权威依据 | 整改建议 |
|----------|----------|----------|----------|----------|----------|
| ARC-001  | 严重/高/中/低/建议 | 精准具体的问题描述 | 架构全局/XX模块 | 对应官方规范/文档链接 | 可落地的整改建议，无代码修改 |
| ...      | ...      | ...      | ...      | ...      | ...      |

---

## 二、程序员视角：代码细节深度审查结果
### 2.1 代码整体质量评估
[项目整体代码质量的综合评价，包含代码优势、核心代码问题]
### 2.2 详细问题与优化建议
| 问题编号 | 风险等级 | 问题所在文件/行号 | 问题描述 | 影响范围 | 权威依据 | 整改建议 |
|----------|----------|--------------------|----------|----------|----------|----------|
| CODE-001 | 严重/高/中/低/建议 | 文件完整路径+对应行号 | 精准具体的问题描述 | XX功能/模块 | 对应编码规范/官方文档链接 | 可落地的整改建议，无代码修改 |
| ...      | ...      | ...                | ...      | ...      | ...      | ...      |

---

## 三、用户视角：需求匹配度审计结果
### 3.1 需求满足度整体评估
[项目需求覆盖、用户体验的综合评价，包含优势、核心匹配问题]
### 3.2 详细问题与优化建议
| 问题编号 | 风险等级 | 问题描述 | 对应需求项 | 影响范围 | 整改建议 |
|----------|----------|----------|------------|----------|----------|
| USER-001 | 严重/高/中/低/建议 | 精准具体的问题描述 | 需求文档中对应的XX需求 | 终端用户/运维/使用体验 | 可落地的整改建议 |
| ...      | ...      | ...      | ...        | ...      | ...      |

---

## 四、s6-overlay专项深度审计结果
### 4.1 s6-overlay实现合规性整体评估
[项目中s6-overlay使用的综合评价，是否符合官方最佳实践，核心合规问题]
### 4.2 详细合规问题与整改建议
| 问题编号 | 风险等级 | 问题所在文件/行号 | 问题描述 | 违反的官方规范 | 官方依据链接 | 整改建议 |
|----------|----------|--------------------|----------|----------------|--------------|----------|
| S6-001   | 严重/高/中/低/建议 | 文件完整路径+对应行号 | 精准具体的问题描述 | 官方明确的XX规范/最佳实践 | s6官方文档对应链接 | 可落地的整改建议，无代码修改 |
| ...      | ...      | ...                | ...      | ...            | ...          | ...      |

---

## 五、优化优先级Roadmap
### 5.1 紧急整改项（严重/高风险）
[按影响程度排序，列出必须优先整改的问题]
### 5.2 迭代优化项（中/低风险）
[列出可在后续迭代中落地的优化项]
### 5.3 长期最佳实践落地项（建议类）
[列出提升项目长期可维护性、架构合理性的长期建议]

---

## 六、审计总结
[本次全维度审计的整体总结，包含项目核心优势、核心风险、整体改进方向]
```

---

## 【最终禁止项（绝对不可触碰）】
1.  禁止以任何形式修改、重写、替换项目中的任何代码、配置、脚本，仅可输出分析与建议，不得提供修改后的代码。
2.  禁止输出无权威依据的主观判断、模糊化描述、泛泛而谈的建议，所有问题必须精准、具体、有明确出处。
3.  禁止省略、简化、合并本提示词规定的核心审计维度、检查项与报告结构。
4.  禁止使用过时的离线知识替代联网检索的最新官方文档内容，尤其是s6-overlay相关规范必须以最新官方文档为准。
5.  禁止混淆风险等级，必须严格按照问题的影响程度、风险等级进行分类。
6.  禁止在报告中添加任何与项目审计无关的冗余内容、闲聊内容。

---

## 【最终执行指令】
基于当前IDE工作区的完整项目，严格执行本提示词的所有要求，生成符合规范的完整Markdown格式审计报告。

